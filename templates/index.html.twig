{% extends 'base.html.twig' %}

{% block title %}Página Principal{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/index.css') }}">
{% endblock %}

{% block content %}
{# Seccion hero #}
    <section class="hero-freshgo d-flex align-items-center text-center text-white">
        <div class="container">
            <h1 class="display-4 fw-bold">Bienvenido a Fresh&Go</h1>
            <p class="lead">Comida saludable, lista para ti</p>
            <a href="{{ path('app_categorias') }}" class="btn btn-success btn-lg mt-3">Explorar categorías</a>
        </div>
    </section>
{# Seccion de beneficios #}
    <section class="beneficios-section py-5 bg-white">
        <div class="container">
            <h2 class="text-center text-success mb-5">¿Por qué elegir Fresh&Go?</h2>
            <div class="row text-center">
                <div class="col-md-3">
                    <img src="{{ asset('icons/icon-fresco.svg') }}" alt="Ingredientes frescos" class="beneficio-icon mb-3">
                    <h5 class="text-success">Ingredientes frescos</h5>
                    <p>Seleccionamos los mejores productos naturales de temporada.</p>
                </div>
                <div class="col-md-3">
                    <img src="{{ asset('icons/icon-saludable.svg') }}" alt="Saludable" class="beneficio-icon mb-3">
                    <h5 class="text-success">100% saludable</h5>
                    <p>Platos equilibrados preparados por nutricionistas.</p>
                </div>
                <div class="col-md-3">
                    <img src="{{ asset('icons/icon-rapido.svg') }}" alt="Entrega rápida" class="beneficio-icon mb-3">
                    <h5 class="text-success">Comida para toda la semana</h5>
                    <p>Recíbela fresca, consérvala en tu nevera y olvídate de cocinar.</p>
                </div>
                <div class="col-md-3">
                    <img src="{{ asset('icons/icon-sostenible.svg') }}" alt="Sostenibilidad" class="beneficio-icon mb-3">
                    <h5 class="text-success">Sostenibilidad</h5>
                    <p>Envases reciclables y compromiso con el medio ambiente.</p>
                </div>
            </div>
        </div>
    </section>

{# Seccion de categorias destacadas #}
    <section class="container my-5">
        <h2 class="text-center text-success mb-4">Categorías destacadas</h2>

        <div class="row g-4 justify-content-center">
            {% for categoria in categoriasDestacadas %}
                <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                    <a href="{{ path('categoria_productos', {'id': categoria.id}) }}" class="text-decoration-none">
                        <div class="card categoria-card h-100 shadow-sm">
                            <div class="categoria-imagen">
                                <img src="{{ asset('uploads/fotos_categorias/' ~ categoria.foto) }}" alt="Imagen de {{ categoria.nombre }}">
                            </div>
                            <div class="card-body text-center">
                                <h5 class="card-title text-success">{{ categoria.nombre }}</h5>
                                {% if categoria.descripcion %}
                                    <p class="card-text text-muted small">
                                        {{ categoria.descripcion|length > 80 ? categoria.descripcion|slice(0, 80) ~ '...' : categoria.descripcion }}
                                    </p>
                                {% endif %}
                            </div>
                        </div>
                    </a>
                </div>
            {% else %}
                <div class="col-12 text-center">
                    <p>No hay categorías destacadas por ahora.</p>
                </div>
            {% endfor %}
        </div>
    </section>


    
{% endblock %}



