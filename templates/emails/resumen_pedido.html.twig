<div style="font-family: Arial, sans-serif; padding: 20px; color: #333;">
    <h2 style="color: #2ecc71;">¡Gracias por tu pedido en Fresh&Go!</h2>

    <p>Hola <strong>{{ usuario.nombre }}</strong>,</p>
    <p>Hemos recibido tu pedido <strong>#{{ pedido.id }}</strong> y lo estamos preparando con mucho cariño.</p>

    <h3>Detalles del pedido:</h3>
    <ul style="list-style: none; padding-left: 0;">
        {% for detalle in detalles %}
            <li style="margin-bottom: 5px;">
                {{ detalle.cantidad }} × <strong>{{ detalle.producto.nombre }}</strong> — {{ detalle.precioUnitario|number_format(2) }}€
            </li>
        {% endfor %}
    </ul>

    <p>Subtotal: {{ pedido.getSubtotal()|number_format(2) }} €</p>
    <p>Coste de envío: {{ pedido.getCosteEnvio()|number_format(2) }} €</p>
    <p><strong>Total: {{ pedido.calcularTotal()|number_format(2) }} €</strong></p>

    <p>
        <strong>Dirección de entrega:</strong><br>
        {{ pedido.usuario.direccion }}
    </p>

    {% set fechaEntrega = pedido.fechaConfirmacion|date_modify('+2 days') %}
    <p><strong>Fecha estimada de entrega:</strong> {{ pedido.fechaConfirmacion|date('d/m/Y') }}</p>

    <hr style="margin: 20px 0;">
    <p style="font-size: 0.9em;">Este correo es tu comprobante de pedido. Gracias por confiar en nosotros</p>
</div>
